# Divide Certo

An Expo React Native app for splitting expenses equally.

## Run locally

```bash
npm install
npm start
```

Then scan the QR code with Expo Go, or press `i`, `a`, or `w` to run on iOS, Android, or web.
